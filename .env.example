# xai | gemini
AI_PROVIDER=gemini

# xAI (only if hackathon credits are available)
XAI_API_KEY=
XAI_MODEL=grok-3-mini

# Gemini free-tier fallback
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Research
EXA_API_KEY=
FIRECRAWL_API_KEY=

# When true, the app stays fully navigable without keys and labels all reports as example data.
DEMO_MODE=false
